#include "mcommon.h"
#include <gtest/gtest.h>
#include "mgfx/app/mgfx_settings.h"

TEST(App, MgfxSettings)
{
//    MgfxSettings::Instance().AddRenderer(std::make_shared<)
    /*
    MgfxSettings::Instance().SetMode(AppMode::Display3D);
    auto mode = MgfxSettings::Instance().GetMode();
    ASSERT_EQ(mode, AppMode::Display3D);

    MgfxSettings::Instance().SetMode(AppMode::Display2D);
    mode = MgfxSettings::Instance().GetMode();
    ASSERT_EQ(mode, AppMode::Display2D);
    */
}